<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>"> <%= @user.followed_users.count %> Following</a> |
  <a href="<%= followers_user_path(@user) %>"><%= @user.followers.count %> Followers</a>
</div>