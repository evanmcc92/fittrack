<div class="row">
	<h1><%= current_user.name %>'s Progress</h1>
	<div class="col-md-8">
		<!-- chart here -->
	</div>
	<div class="col-md-4">
	<% if @workout %>
		<%= render 'exercise' %>
	<% else %>
		<p>You do not have any workouts</p>
	<% end %>
	</div>
</div>

